# GitHub License Scanner dependencies
# Python 3.11+ recommended (tomllib is in the standard library)

nicegui>=2.0.0
httpx>=0.27.0
packaging>=24.0
python-dotenv>=1.0.0
# Optional: only needed to regenerate docs/audit reports
# reportlab>=4.0.0
